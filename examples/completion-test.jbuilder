# Test auto-completion features
# Type "# @op" and press Ctrl+Space to see snippets

# Type "@openapi id:" and press space to see type completions
# @openapi id:

# Type "@openapi name:string " and see attribute completions
# @openapi name:string 

# Type "@openapi status:string required:" and see boolean completions
# @openapi status:string required:

json.id @user.id
json.name @user.name
json.status @user.status